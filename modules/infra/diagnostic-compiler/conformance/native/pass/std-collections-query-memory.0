pub fn main(world: World) -> Void raises {
    let values: [2]u8 = [1_u8, 2_u8]
    if std.collections.contains(values, 2_usize, 2_u8) {
        check world.out.write("std collections query memory ok\n")
    }
}
