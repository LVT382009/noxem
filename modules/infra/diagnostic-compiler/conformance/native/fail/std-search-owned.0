type File {
    fd: i32,
}

fn close(file: owned<File>) -> Void {
}

pub fn main() -> Void {
    let files: [1]owned<File> = [File { fd: 1 }]
    let file: owned<File> = File { fd: 1 }
    let _found: usize = std.search.indexOf(files, file)
    close(file)
    close(files[0])
}
